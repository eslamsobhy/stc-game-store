import React from "react";

// styling
import "./loading.scss";

const Loading = () => {
  return (
    <section className="section spinner-container">
      <div className="loading-spinner"></div>
    </section>
  );
};

export default Loading;
